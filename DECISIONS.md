# 📐 قرارات التصميم (Architecture Decision Records)

وثيقة توثّق القرارات الهندسية المهمة وأسبابها. مرتبة من الأحدث للأقدم.

---

## ADR-009: الترميز في سكربتات Windows (قرار تشغيلي)
**الحالة:** مُعتمد
- **القرار:** في أي أتمتة مستقبلية تتلاعب بملفات TS عربية تحت PowerShell 5.1، استخدم `ReadAllBytes`/`WriteAllBytes` مع UTF-8 صريح (أو أدوات القراءة/الكتابة غير النصية)، ولا تستخدم `Get-Content`/`Set-Content`.
- **السبب:** `Get-Content` أعاد ترميز التعليقات العربية إلى CP1252 وأفسد ~77 ملفاً أثناء استبدال `console.*`→`logger.*`. عُكس الضرر بسكربت `recover-encoding.ps1`.
- **الاحتفاظ:** `C:\Users\MSi\AppData\Local\Temp\opencode\recover-encoding.ps1`.

## ADR-008: وحدة AutoMod وليس تمديد antiSpam
**الحالة:** مُعتمد
- **القرار:** فصل الفلترة الذكية في `src/modules/autoMod.ts` مستقلة عن Anti-Spam القديم (الذي يكتشف التكرار فقط).
- **السبب:** AutoMod يشمل أنواعاً مختلفة من الانتهاكات (كلمات/حروف كبيرة/إشارات/إيموجي) مع نظام نقاط تحذير وإجراءات تدريجية، وهو أنظف كوحدة منفصلة.

## ADR-007: i18n بقواميس محلية ثابتة وليس مكتبة خارجية
**الحالة:** مُعتمد
- **القرار:** ملفا `translations.ts` (ar/en) ودالة `t()` بسيطة بدل إدخال مكتبة مثل i18next.
- **السبب:** حجم المشروع معقول، والوظيفة المطلوبة (ترجمة + استبدال متغيرات + fallback) تغطيها 30 سطراً؛ يبقي الاعتماديات خفيفة ويُسهّل الفحص بالأنواع.

## ADR-006: بطاقات Rank عبر `canvas` وليس `@napi-rs/canvas`
**الحالة:** مُعتمد
- **القرار:** `rankCards.ts` يستخدم `canvas` (نفس مكتبة welcomeCards)، رغم وجود `@napi-rs/canvas` في dependencies.
- **السبب:** اتساق واحد للرسم في كل البوت وتجنب مكتبتين متوازيتين للرسم؛ `canvas` يدعم `roundRect` و`shadowBlur` المطلوبة للبطاقة.

## ADR-005: حفظ السحوبات في JSON والاسترجاع عند الإقلاع
**الحالة:** مُعتمد
- **القرار:** `data/giveaways.json` مع `restoreGiveaways(client)` في `index.ts` بدل الاعتماد على الذاكرة.
- **السبب:** السحب قد يطول (ساعات/أيام) ويجب أن ينجو من إعادة التشغيل؛ JSON متوافق مع طبقة التخزين الحالية.

## ADR-004: نسخ التذاكر إلى ملف نصي + DM
**الحالة:** مُعتمد
- **القرار:** عند إغلاق التذكرة تُبنى نسخة نصية (آخر 100 رسالة) تُرسل DM لصاحبها وتُحفظ في `data/transcripts/`.
- **السبب:** أرشفة سريعة بدون قاعدة بيانات خارجية، وضمان وصول المستخدم لسجل محادثته حتى لو حُذفت القناة.

## ADR-003: لوقات الإدارة عبر `modActionLog` موحد
**الحالة:** مُعتمد
- **القرار:** جدول `MOD_ACTION_META` واحد يصف كل نوع (ban/kick/warn/timeout/...) يمر عبر `modActionLog()` بدل تكرار embeds.
- **السبب:** إضافة إجراء جديد = سطر واحد؛ اتساق الألوان/الأيقونات/الصيغة؛ ويسّر ربط dashboard لاحقاً.

## ADR-002: تذاكر بالتأكيد المزدوج (زر إلغاء)
**الحالة:** مُعتمد
- **القرار:** الإغلاق ينتظر 10 ثوانٍ مع زر `ticket_cancel_close` عبر collector و`pendingCloses` يمنع التنفيذ المزدوج.
- **السبب:** منع إغلاق التذكرة بالخطأ (الشكوى الأكثر شيوعاً) مع ضمان عدم تنفيذ الإغلاق مرتين بعد انتهاء الـ collector.

## ADR-001: Middlewares موحدة بدل تكرار فحوصات الصلاحيات
**الحالة:** مُعتمد
- **القرار:** كل أمر يصرّح بصلاحياته (ownerOnly/adminOnly/cooldown) و`interactionCreate.ts` ينفذها مركزيّاً.
- **السبب:** حذف التكرار في ~40 أمراً، وسهولة إضافة سلوك (مثل cooldown) لكل الأوامر من مكان واحد.
